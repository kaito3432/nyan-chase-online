# にゃんチェイス 開発基盤版

現在の Ver1.5.2 を、今後の CPU 対戦・オンライン対戦に向けて複数ファイルへ整理した版です。

## ファイル構成

- `index.html` — 画面の骨組み
- `style.css` — デザイン
- `engine.js` — ゲームルール
- `game.js` — UI・ゲーム進行
- `animation.js` — アニメーション
- `audio.js` — 効果音・設定
- `assets/images/` — 今後のイラスト
- `assets/audio/` — 今後のBGM・音素材

## この版で修正したバグ

柴犬が探索を開始した後、クンクン演出中にもう一度盤面を操作して
「探索＋移動」の2回行動ができてしまう問題を修正しました。

探索開始時点でその柴犬を `探索中` として確定し、
`actionLocked` が有効な間は盤面・柴犬カード・ターン終了操作を受け付けません。

## GitHub Pages へのアップロード

ZIPを展開し、`nyan_chase_project` フォルダの「中身」を
GitHub のリポジトリ直下へアップロードしてください。

リポジトリ直下が次のようになればOKです。

```text
index.html
style.css
engine.js
game.js
animation.js
audio.js
assets/
```

`index.html` がさらに一段下のフォルダに入らないようにしてください。


## Ver1.5.3 追加内容

- BGMを追加（外部音源なしの軽量Web Audio版）
- 設定画面で BGM ON/OFF
- 設定はブラウザに保存
- 9〜11ターンはBGMが少しだけ緊張感のあるパターンへ変化
- 勝利演出時はBGMを一時的に小さくして効果音を聞きやすく調整
- iPhoneなどの自動再生制限に対応し、最初のユーザー操作後からBGM開始


## Ver1.6

- 「にゃんチェイス」公式アートテイストを実装開始
- ロゴを画像化
- ネコ・柴犬3匹をイラスト素材へ変更
- ダンボール・足跡・スタート地点を画像素材へ変更
- 盤面背景にダンボール街イラストを薄く追加
- BGM初期音量をアップ
- BGM音量スライダーを追加（設定をブラウザ保存）
- 探索中の二重行動ロックは継続


## Ver1.7

- ロゴを `object-fit: contain` ベースで再設計し、画面からはみ出さないように修正
- iPhoneを含む縦持ちスマホで、基本的にスクロールせず1画面完結するレスポンシブUIへ変更
- 設定ボタンを右上のコンパクトな歯車アイコンへ移動
- ステータス、操作ガイド、盤面、犬カード、メッセージ、下部ボタンの高さを画面サイズに合わせて自動調整
- 柴犬選択時の発光、移動可能交差点、探索可能箱のアニメーションを強化
- 足跡発見時のポップ、箱開閉、クンクン終了時のキラキラを強化
- BGM音量スライダー、探索中の二重行動ロックは継続


## Ver1.8 CPU対戦 第1弾

- 起動時にモード選択画面を追加
- 対人戦はこれまで通り利用可能
- CPU対戦を追加（プレイヤー＝ネコ / CPU＝柴犬警察）
- CPU柴犬は中央16交差点へ自動配置
- CPUは3匹が固まりすぎないように移動
- 発見済みの足跡周辺を優先して探索
- 足跡が無い序盤は中央寄りを広く捜査
- CPUの行動中は入力ロック
- オンライン対戦は「準備中」として表示
- ロゴ / 設定位置 / 1画面UIを再調整


## Ver1.8a 修正

- スタート画面をスマホ向けに全面再設計
- ロゴが必ず画面内に収まる `object-fit: contain` レイアウトへ変更
- 対人戦 / CPU対戦カードを大きくタップしやすく変更
- iPhone Safari向けに `pointerup` / `touchend` / `click` のフォールバックを追加
- タップの二重発火を抑止
- タイトル画面から設定・遊び方・サウンド切替が可能
- 対人戦・CPU対戦タップ時にゲームが確実に開始するよう修正


## Ver1.8b

- CPU警察の思考を強化
- CPUは毎ターン最低1回は探索を実行
- あか柴＝探索寄り / みどり柴＝バランス / あお柴＝移動寄り の役割分担
- 未探索の箱を優先し、空振りした箱も記憶して再探索を避ける
- 足跡を発見した後は周辺を強く追跡
- CPU探索回数 / 発見足跡数 / 残りターンを表示
- 11ターン目の表示崩れを修正
- ホーム画面とプレイ中UIを承認デザインに寄せて再調整


## Ver1.9 Phase1

- 0ターン目：柴犬警察3匹を先に配置
- 逃走1ターン目：ネコが警察配置を見てスタート地点を選択
- 以降は ネコ移動 → 警察捜査 の繰り返し
- ネコ側表示：緑＝安全 / 赤⚠️＝次ターン行き止まり / グレー＝移動不可
- 上部UIを「残りターン / フェーズ」に整理
- 操作ガイドを1か所に統一
- 探索回数などの重複表示を削除
- 「最初からやり直す」を設定画面へ移動
- 設定に「タイトルへ戻る」を追加
- ネコターン中は柴犬カードを非表示
- 発見した足跡だけをコンパクト表示


## Ver1.9 Phase2

- 警察配置後、ネコが25個すべてのダンボールから任意のスタート地点をタップ選択できるよう修正
- ネコ潜伏フェーズでは交差点タップを無効化し、箱タップを優先してスマホ誤操作を防止
- 残り3ターンからBGMを確実に緊張感のある終盤パターンへ切り替え
- 終盤は残りターン表示も赤系で強調
- ネコ潜伏 / ネコ移動 / 柴犬捜査のフェーズ切り替えカード演出を追加
- 柴犬カードを下からスライドイン
- ネコ側ボタンをフェードイン
- スタート地点候補のダンボールをピンク枠で明示


## Ver1.9 Phase3 - CPU警察AI強化

- CPU警察をスコア評価型AIへ変更
- 未探索箱、空振り箱、発見済み足跡を記憶
- 毎ターン最低1回は探索
- 足跡発見後は周辺の推定逃走ルートを重点捜査
- 3匹が固まりすぎないよう分散
- 直前の交差点へ戻る往復移動を抑制
- 動的役割：追跡 / 探索 / 封鎖
- 終盤3ターンは封鎖・包囲の評価を上げる
- 行き止まり候補や逃げ道の少ない箱を封鎖対象として評価
- CPU行動前に「考え中」風の短い思考表示
- CPUの行動テンポを改善


## Ver1.9 Phase3.5

### CPU難易度選択
CPU対戦を選ぶと、開始前に3段階から選択。

- やさしい：判断のランダム性を増やし、ときどき読み違える
- ふつう：Phase3標準AI
- つよい：足跡追跡・分散・往復防止・終盤包囲をさらに強化

### ネコ初期潜伏バグ修正
- ネコ潜伏フェーズではダンボールを交差点より前面に配置
- 交差点のタップ受付を完全停止
- 各箱に pointerdown / touchstart を直接設定
- タップした箱番号をそのままスタート地点として保存


## Ver1.9 Phase3.6 - CPU再調整

### ふつう
- 原則として1ターンに2匹が探索を優先
- 未探索箱の価値を大幅アップ
- 空振り済みの箱を強く避ける
- 足跡発見後は周辺探索を強化
- 情報が増えない無駄移動を減らす

### つよい
- ネコ候補位置の内部確率マップを追加
- 確率の高い箱を優先探索
- 逃げ道が少ない候補を先回りして封鎖
- 終盤3ターンの包囲をさらに強化
- 行動のランダム性をほぼ排除

### やさしい
- 初心者向けとして、移動や読み違いを残す


## Ver1.9 Phase3.6a バグ修正

- CPU警察の3匹目が「探索」を行ったとき、CPUターンが `waitingEnd` で停止してネコ側へ戻らない不具合を修正。
- CPU戦では3匹の行動完了後も一度 `dogs` フェーズを維持し、`runCpuPoliceTurn()` が全員行動済みを検出して `cpuFinishTurn()` を呼ぶように変更。
- 3匹目が「移動」の場合・「探索」の場合のどちらでも、CPU捜査終了後にネコターンへ正常に移行する設計に統一。


## Ver1.9 Phase4-1 - CPUネコ基本実装

- CPU対戦で「ネコで遊ぶ / 警察で遊ぶ」を選択可能
- プレイヤーが警察側の場合、CPUネコが自動でスタート地点を選択
- CPUネコは毎ターン自動で移動
- やさしい：ランダム性多め
- ふつう：柴犬から距離を取り、逃げ道の多い箱を優先
- つよい：1手先の逃走余地まで評価して行き止まりを強く回避
- プレイヤー警察は従来通り3匹を1匹ずつ操作して探索 / 移動


## Ver1.9 Phase4-1a バグ修正

- ホーム画面のCPU対戦説明を「ネコ / 警察 どちらでも遊べる」に変更。
- プレイヤーが警察側のCPUネコ戦で、CPUネコの初期潜伏位置が見える問題を修正。
- CPUネコの移動アニメーションを警察側では表示せず、内部位置だけ更新するよう変更。
- `cpuCat` モードではネコ本体・ネコの非公開足跡を防御的に描画しないよう制限。
- CPUネコの「隠れる / 移動する」情報は警察側に漏れず、「移動した」ことだけ通知。


## Ver1.9 Phase4-2

### CPUネコAI強化
- ふつう：警察3匹との距離、逃げ道数、次ターンの警察圧力、1手先の逃走余地を評価
- つよい：2手先の逃走候補まで評価し、包囲されやすい箱を強く回避
- つよい：警察が次に圧力をかけられる箱を予測
- 初期潜伏位置も難易度に応じてより賢く選択

### ゲーム終了後
- プレイヤーが警察側のCPUネコ戦では、勝敗に関係なくネコの全移動軌跡を盤面上に公開
- START → 各ターン → 最終地点を順番付きで表示

### ターンUI
- 左上を「現在ターン / 11」形式へ変更
- 8〜9ターンはオレンジ、10ターンは赤寄り、11ターンは強調表示


## Ver1.9 Phase4-2a ホットフィックス

- iPhone Safari / GitHub Pagesで古い `game.js` と新しいHTML/CSSが混在するキャッシュ問題を防ぐため、
  CSS・JSすべてにバージョンクエリを追加。
- 初期ターン表示を `0 / 11` に統一。
- 盤面描画後に「25箱 + 36交差点」が存在するか確認し、不完全な場合は自動で1回再描画。
- ダンボールと交差点の表示状態をCSS側でも防御的に固定。


## Ver1.9 Phase4-2b 盤面表示修正

- iPhone Safariで盤面が背景だけになり、箱・交差点が表示されない問題を修正。
- 原因になり得る `calc()` 内の掛け算・割り算を廃止。
- 25箱と36交差点をJavaScriptから単純なパーセント座標で配置。
- CSS/JSのキャッシュバージョンを `1.9.4.2b` に更新。


## Ver1.9 Phase4-2c 起動バグ修正

- 盤面が背景だけになる本当の原因を修正。
- `game.js` が参照していた `routeRevealPanel` / `routeRevealSub` がHTMLに存在せず、
  初期化時にJavaScriptが停止していた。
- ネコ逃走ルート公開用パネルをHTMLへ追加。
- ルート表示UIが欠けてもゲーム本体を止めないnullガードを追加。
- CSS/JSキャッシュバージョンを `1.9.4.2c` に更新。


## Ver1.9 Phase4-2d 逃走ルート公開修正

- CPUネコの移動履歴を `cpuCatRoute` として専用保存。
- スタート地点と毎ターンの移動先を確実に記録。
- ゲーム終了後の最終 `render()` がルート表示を消してしまう問題を修正。
- ゲーム終了画面描画後にルートを表示する順序へ変更。
- 実際のダンボールDOM位置から座標を計算して、START→各ターンの線と番号を盤面に表示。
- ルート線・番号バッジのz-indexと視認性を強化。


## Ver1.9 Phase4-2e 逃走ルート表示・再修正

- 前版ではルート公開処理がゲーム終了関数ではなく設定の再スタート処理側へ誤って入っていたため、ゲーム終了時に表示されていなかった。
- CPUネコ戦（プレイヤー警察）の結果画面内に、25箱のミニ盤面を直接追加。
- ゲーム終了と同時に START → 各ターン → 最終地点を線と番号で表示。
- 勝敗に関係なく必ず表示。
- 盤面側のルートオーバーレイも同時に生成。


## Ver1.9 Phase5 - 最終仕上げ

- 対人戦でもゲーム終了時にネコの全移動軌跡を結果画面で公開。
- CPUネコ戦と同じく START → 各ターン → 最終地点を表示。
- 11ターン目に専用「LAST TURN」バナー、SE、振動を追加。
- 足跡発見時の箱フラッシュ演出を追加。
- 空振り探索時の軽いフェード演出を追加。
- 柴犬カードの登場アニメーションを微調整。
- 勝敗結果モーダルの背景・祝福演出を強化。
- 終盤BGM切り替え、BGM音量設定、CPU両陣営、難易度選択など既存機能は維持。


## Ver1.9 Phase5b ホーム画面操作不能バグ修正

- 原因を特定：`lastTurnStingerPlayed=false` が `let lastTurnStingerPlayed` の宣言より前に実行されていた。
- JavaScript読み込み直後に TDZ（Temporal Dead Zone）の `ReferenceError` が発生し、
  ホーム画面のボタンイベントが登録される前に `game.js` が停止していた。
- 宣言前の代入を削除し、`initGame()` 内でのみリセットするよう修正。
- 音声アンロック処理が失敗してもボタン操作を止めないよう `bindPress()` を防御化。
- キャッシュバージョンを `1.9.5b` に更新。


## Ver1.9 Visual Update 1 — タイトル画面刷新
- Phase5b のゲームロジックは維持したままタイトル画面のみ刷新。
- タイトル画面の絵文字UIを廃止。
- 既存のネコ・柴犬3匹を大きく使ったキャラクターヒーロー構成へ変更。
- 対人戦 / CPU対戦は既存IDとイベントを維持し、タップ動作を壊さない構成。
- 設定・遊び方・サウンド・オンラインロックはCSSアイコン化。
- 小型スマホ向けの高さ調整を追加。


## Ver1.9 Visual Update 1a — タイトル画面UI修正
- 上部ロゴ / ネコ / 柴犬3匹の画像を `object-fit: contain` に統一し、見切れを修正。
- 「遊び方を選んでね」の左右肉球を画像からCSS描画へ変更し、掠れを解消。
- 対人戦・CPU対戦のキャラクター画像を大きな表示領域に変更し、切り取りを解消。
- サウンドアイコンをSVGスピーカーに変更。
- 設定アイコンを視認性の高いSVG歯車に変更。
- ランキングを削除。
- 「タイトルを出る」は追加しない。
- 下部メニューは「遊び方 / サウンド / 設定」の3項目に整理。


## Ver1.9 Visual Update 1b — スクリーンショット寄せ
- ユーザー提示の完成イメージに近い縦長タイトル構成へ変更。
- 上部に大きなロゴ、その下にネコ＋柴犬3匹を大きく配置。
- 対人戦 / CPU対戦 / オンラインを大型カード化。
- 下部は「遊び方 / サウンド / 設定」の3項目のみ。
- 「タイトルを出る」とランキングは未実装。
- 既存キャラ画像は object-fit: contain で見切れを防止。


## Ver1.9 Visual Update 1c — プレイ中専用アイコン実装
- プレイ中のネコ・柴犬3匹を、ホーム用素材とは別の専用アイコンに分離。
- ネコ：通常 / アクション / 警戒 の3素材を個別PNG化。
- 柴犬：赤柴 / 緑柴 / 青柴を個別PNG化。
- 現在のゲーム盤面と柴犬カードでは専用アイコンを使用。
- ホーム画面の大きなキャラクター素材は従来のまま維持。


## Ver2.0 Phase1 — PWA化

追加内容:
- `manifest.webmanifest`
- `service-worker.js`
- 192px / 512px / maskable / Apple Touch Icon
- iPhoneの「ホーム画面に追加」に対応
- ホーム画面アイコンから standalone 表示で起動
- HTMLはネットワーク優先、画像/CSS/JSはキャッシュ優先
- 一度読み込んだ後は主要ファイルをオフラインキャッシュ
- GitHub Pages のサブディレクトリ配置でも動くよう相対パスで構成

### iPhone確認手順
1. Safariでゲームを開く
2. 共有ボタンをタップ
3. 「ホーム画面に追加」
4. 追加された「にゃんチェイス」アイコンから起動
5. Safariのアドレスバーなしでゲームが起動することを確認


## Ver2.0 Phase3 — CPU最終調整

### プレイヤー＝警察 / CPU＝ネコ
- やさしい: CPUネコの3・6・9ターン目の位置に、ターン数字付き足跡を自動公開
- ふつう: 6ターン目のみ、ターン数字付き足跡を自動公開
- つよい: 自動ヒントなし（探索で見つけた痕跡のみ）
- 通常探索で発見した足跡にもターン番号を表示

### プレイヤー＝ネコ / CPU＝警察
- やさしいは探索判断の揺らぎを少し増やして遊びやすく調整
- ふつうは探索・痕跡追跡を少し強化
- つよいは従来の強い探索ロジックを維持

### ネコ移動の危険判定
従来は「次ターンに出口があるか」だけを見ていました。
Phase3では現在ターンから11ターン目までをDFSで逆算し、
未訪問の箱だけを使って最後まで逃げ切れるルートが1本でも存在するか判定します。
存在しない移動先は、その場ですぐ行き止まりでなくても赤⚠️表示になります。


## Ver2.0 Phase3.1 — 危険表示 / 足跡表示修正
- ⚠️が全マスに出る不具合を修正。
  - 危険判定は「現在ネコが実際に移動できる候補マス」だけに限定。
  - その候補から11ターン目まで逃げ切れるルートが無い場合のみ赤⚠️表示。
- CPUネコの足跡の自動公開を撤廃。
- 警察が実際に探索して足跡を発見した時だけ表示。
- 発見した足跡には、その足跡が何ターン目のものか数字を表示。


## Ver2.0 Phase3.2 — ラストターン / 足跡数字修正

### 11ターン目
- プレイヤーがネコ側で、11ターン目に行き止まりへ移動しても即敗北しない。
- 12ターン目は存在しないため、11ターン目は移動後に警察の最終捜索へ進む。
- 警察が11ターン目の捜索でネコを発見できなければネコ勝利。

### 発見済み足跡のターン数字
数字は足跡を実際に探索で発見した場合のみ、難易度に応じて表示。
- やさしい：3 / 6 / 9ターン目だけ数字表示
- ふつう：6ターン目だけ数字表示
- つよい：数字表示なし

それ以外の発見済み足跡は、足跡マーク自体は表示するが数字は付けない。


## Ver2.0 Phase4 — サウンド実装

正式採用BGM:
- ホーム: HOME1 Welcome to Nyan Chase
- 通常ゲーム: P1 Pop Hide and Seek
- 9〜11ターン: T1 Last Three Turns

BGM切替:
- タイトル画面 = home
- 対戦開始 = normal
- 残り3ターン = tension
- ゲーム終了時 = BGM停止 → 勝者別ジングル

SE:
- ネコ/柴犬移動
- 探索
- 足跡発見
- 捕獲
- ターン切替
- UIボタン
- 移動不可
- ゲーム開始
- ネコ勝利 / 警察勝利ジングル

iOS Safariのユーザー操作制限に合わせ、最初のタップで音声再生をアンロックします。
PWAキャッシュにも全音源を追加済み。


## Ver2.0 Phase4.1 — サウンド修正
- ホーム画面で「遊び方 / サウンド / 設定」等をタップしてもBGMをリセットしない。
- ゲーム中も通常タップではBGMをリセットしない。
- BGM曲の切替は「対戦開始」「9〜11ターン」「ゲーム終了」など状態変化時だけ。
- SEを電子音中心からフォーリー寄りへ変更。
  - 移動: ササッ
  - 探索: ダンボールのガサガサ
  - 捕獲: 柔らかいドン＋ガサッ
  - ボタン: 小さな物理クリック
  - 移動不可: ダンボールのコツッ
- 探索時「クンクン……」ポップ表示を750msから360msへ短縮。


## Ver2.0 Phase4.2 — 探索フリーズ / タップ音 / SE音量修正

- 探索開始時に `animation.js` 内の不要な `Nyan` 識別子が実行され、
  ReferenceErrorで探索処理が止まり `actionLocked` が解除されない不具合を修正。
- 探索開始時は専用のダンボール「ガサガサ」SEを再生。
- 設定、サウンド、柴犬選択など通常UIのタップ音を廃止。
- 汎用 `Audio.play("tap")` 呼び出しをゲーム側から削除。
- SFXを全体的に小さく調整。
  - 移動 / 探索 / UI系は約25%前後
  - 足跡発見は約30%
  - 捕獲は約34%
  - 勝利ジングルは約46%
- SFXをONへ戻した瞬間にも確認音を鳴らさない。


## Ver2.0 Phase4.3 — SE音量 / クンクン表示調整
- 移動SEを Phase4.2 からさらに約50%低減。
  - move / cat: 0.25 → 0.125
- 探索時「クンクン……」ポップ表示を約1/3へ短縮。
  - 360ms → 120ms
- ポップのフェードも50msへ短縮。


## Ver2.0 Phase4.4 — 全SE音量 / 探索演出 再調整
- 全SEをPhase4.3比でさらに約50%ダウン。
- 移動SE: 0.125 → 0.0625。
- 探索SE: 0.26 → 0.13。
- 足跡・捕獲・勝利ジングルなども全体的に半減。
- 「クンクン……」文字だけでなく探索演出全体を短縮。
  - 文字表示: 90ms
  - 箱を開く開始: 90ms
  - 犬の探索ポップ全体: 820ms → 280ms
- PWAキャッシュ名も更新。

## Ver2.0 Phase4.5
- スクリーンショットで指定された大きな探索結果ポップ
  「クンクン…… / 何もないワン！」の表示時間を1500ms → 450msへ短縮。
- Phase4.4から全SE音量をさらに50%ダウン。
- PWAキャッシュを更新。

## Ver2.0 Phase4.6
- 小さい「クンクン」探索アニメーションを元の自然な速度へ復元。
  - motion status: 750ms
  - 箱オープン開始: 320ms
  - 探索アニメーション全体: 820ms
- 画面中央の大きな探索結果ポップ
  「💨 クンクン…… / 何もないワン！」を450ms → 900msへ変更。
- Phase4.5で下げたSE音量はそのまま維持。


## Ver2.0 Phase5 — 勝敗演出
- ネコ勝利専用カットインを実装。
- 柴犬警察勝利専用カットインを実装。
- 決着後: 約0.28秒の間 → カットイン → 結果画面 → ネコの逃走軌跡。
- カットインは約1.85秒で自動終了。タップでスキップ可能。
- Phase4の勝者別ジングルと同期。
- 既存の結果画面・ルート表示へそのまま接続。
- CPU警察戦を含め、履歴がある場合は結果画面で逃走ルートを表示。
- PWAキャッシュへカットイン画像を追加。

追加画像:
- assets/images/cutin_cat_win.jpg
- assets/images/cutin_police_win.jpg


## Online β1 — 合言葉コード「部屋作成」実装

ホーム画面の「オンライン対戦」を有効化。

今回の実装範囲:
- 「オンライン対戦」を開く
- 「部屋を作る」
- Cloudflare Workerへ `POST /api/rooms`
- 6桁の合言葉コードを表示
- コードをクリップボードへコピー
- ホスト側WebSocket接続
- 「相手を待っています…」表示
- presenceで2人揃ったことを検知できる土台

まだ未実装:
- コード入力で部屋へ参加するUI
- ネコ / 警察の役割決定
- 盤面・ターン・探索・勝敗の同期

### API URL
開発用URLは `online.js` の `API_BASE` 1か所にのみ定義。
App Store / 本番公開前に、個人情報を含まない独自APIドメイン等へ差し替える。
